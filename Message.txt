Pull về nhớ chạy

//
Kiểm tra xem có pull về được file dockerignore bên backend nếu k có thì kêu mình gửi nhé
Kiểm tra docker-file
Kiểm tra docker-comppose.dev.yml và build lại docker bằng lệnh  docker-compose -f docker-compose.dev.yml up -d --build (Phát triển)
Kiểm tra docker-comppose.yml và build lại docker bằng lệnh  docker-compose -f docker-compose.yml up -d --build (sản phẩm)
Vì có thêm phần queue:work cho bên chat nên phải build lại cho docker tự chạy nhé

Kiểm tra đầy đủ các file nói trên nếu thiếu file nào thì nhớ bảo gửi. 

cd vào src bên backend chạy composer i
bên frontend thì npm run dev
Chạy xong test chức năng nt tin nhé!